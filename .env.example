OPENAI_API_KEY=sk-proj-your-openai-key-here
GOOGLE_API_KEY=AIzaSy-your-google-api-key-here
TAVILY_API_KEY=tvly-your-tavily-key-here
PEXELS_API_KEY=your-pexels-api-key-here

# Database Configuration (PostgreSQL when using Docker / Postgres, leave empty for SQLite)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_content_factory

# API access control (both optional; unset = open, which is fine on localhost only)
# API_KEY: when set, callers must send it as the X-API-Key header. The frontend
#          reads the same value from VITE_API_KEY (see frontend/.env.example).
# API_KEY=choose-a-long-random-string
# ALLOWED_ORIGINS: comma-separated CORS allowlist. Defaults to the local Vite
#                  dev server origins. Use "*" only for a throwaway demo.
# ALLOWED_ORIGINS=http://localhost:3000

# Model configuration (all optional; defaults shown)
# LLM_FAST_MODEL:    router, research extraction, topic guard, campaign copy.
# LLM_QUALITY_MODEL: QA auditor, revision agent, podcast scripter, and the
#                    fallback used when no model is chosen in the UI.
# LLM_FAST_MODEL=gpt-5-mini
# LLM_QUALITY_MODEL=gpt-5-mini
#
# LLM_JUDGE_MODEL is read ONLY by the evaluation node (in-house G-Eval and
# DeepEval). It defaults to LLM_QUALITY_MODEL, so leaving it unset changes
# nothing. Set it to run an INDEPENDENT-JUDGE evaluation arm without also
# moving the QA auditor and revision agent, which read LLM_QUALITY_MODEL and
# would otherwise confound the comparison:
#     LLM_JUDGE_MODEL=gpt-4o pytest tests/golden -v -s
# LLM_JUDGE_MODEL=gpt-4o

# Optional
# HF_TOKEN=hf_your-huggingface-token-here
# FIRECRAWL_API_KEY=fc-your-firecrawl-key-here
# TWITTER_API_KEY=your-twitter-api-key-here
