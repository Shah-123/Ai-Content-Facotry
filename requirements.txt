# ============================================================================
# AI Content Factory — Direct Dependencies
# ============================================================================
# Install:  pip install -r requirements.txt
#
# This is a curated list of DIRECT dependencies only. Transitive dependencies
# (langchain-core, pydantic-core, certifi, etc.) are resolved automatically by pip.
#
# VERSION RANGES: every entry is bounded on BOTH sides.
#   - The lower bound is the oldest version known to work.
#   - The upper bound excludes the next major release, so a breaking upstream
#     release cannot silently change behaviour between two installs of this
#     file. (Previously every pin was open-ended `>=`, which meant a fresh
#     install months apart could resolve to different major versions.)
#
# For an EXACT, reproducible install — the versions this project was
# developed, tested and demonstrated against — use the lockfile instead:
#     pip install -r requirements.lock.txt
# ============================================================================

# --- LangGraph & LangChain (core orchestration) ---
langgraph>=1.2.0,<2.0.0
langgraph-checkpoint>=4.1.0,<5.0.0
langgraph-checkpoint-sqlite>=3.1.0,<4.0.0
langgraph-checkpoint-postgres>=3.1.0,<4.0.0
psycopg[binary]>=3.3.0,<4.0.0
psycopg-pool>=3.2.0,<4.0.0
langgraph-prebuilt>=1.1.0,<2.0.0
langchain>=1.3.0,<2.0.0
langchain-openai>=1.3.0,<2.0.0
langchain-tavily>=0.2.0,<0.3.0
langchain-community>=0.4.0,<0.5.0

# --- Web API ---
fastapi>=0.115.0,<0.140.0
uvicorn>=0.30.0,<0.52.0
python-multipart>=0.0.26,<0.1.0
sse-starlette>=3.0.0,<4.0.0
aiofiles>=23.0.0,<26.0.0

# --- Data & Validation ---
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# --- LLM Providers ---
openai>=2.0.0,<3.0.0
google-genai>=2.0.0,<3.0.0
tavily>=1.1.0,<2.0.0

# --- Web Research ---
beautifulsoup4>=4.12.0,<5.0.0
requests>=2.32.0,<3.0.0

# --- Document Ingest & RAG Vector DB (ChromaDB) ---
pypdf>=6.0.0,<7.0.0
chromadb>=1.5.0,<2.0.0

# --- Video Generation ---
moviepy>=2.0.0,<3.0.0
# openai-whisper uses date-based versions (e.g. 20250625), not semver, so a
# major-version cap is meaningless here. The lockfile pins the exact build.
openai-whisper>=20240930
imageio-ffmpeg>=0.5.0,<0.7.0
pillow>=11.0.0,<13.0.0
numpy>=1.26.0,<3.0.0

# --- Blog Export Formats ---
markdown>=3.5.0,<4.0.0
python-docx>=1.0.0,<2.0.0
reportlab>=5.0.0,<6.0.0

# --- Utilities ---
tiktoken>=0.7.0,<1.0.0
tenacity>=9.0.0,<10.0.0

# --- Academic Evaluation (official G-Eval, Liu et al. 2023) ---
deepeval>=4.0.0,<5.0.0

# --- Testing ---
pytest>=9.0.0,<10.0.0
pytest-asyncio>=1.0.0,<2.0.0
